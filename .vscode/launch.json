{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Local Config",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "justMyCode": true,
            "args": [
                "--config",
                "${workspaceFolder}/local"
            ]
        }
    ]
}